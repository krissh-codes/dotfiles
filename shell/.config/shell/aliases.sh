# !sh /bin/bash
alias ls='lsd'
alias l='ls -l'
alias la='ls -a'
alias lla='ls -la'
alias lt='ls --tree --depth=2'
alias lst='ls --tree --depth=2'

alias ports='lsof -i -P | grep LISTEN | grep :$PORT'
alias got='git'
alias gt='git'

alias search="find . -iname"
alias cls="clear"
