# !sh /bin/bash
alias ls='lsd'
alias l='ls -l'
alias la='ls -a'
alias lla='ls -la'
alias lt='ls --tree'

alias ports='lsof -i -P | grep LISTEN | grep :$PORT'
alias got='git'
alias gt='git'

alias search="find . -iname"
alias cls="clear"
